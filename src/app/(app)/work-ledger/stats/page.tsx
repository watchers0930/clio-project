import { WorkLedgerStats } from '@/components/work-ledger/work-ledger-stats';

export default function WorkLedgerStatsPage() {
  return (
    <div className="work-ledger-page flex flex-1 flex-col min-h-0 px-8 py-8 w-full">
      <WorkLedgerStats />
    </div>
  );
}
