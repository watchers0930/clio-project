import { WorkLedgerView } from '@/components/work-ledger/work-ledger-view';

export default function WorkLedgerPage() {
  return (
    <div className="flex flex-1 flex-col min-h-0 px-8 py-8 w-full">
      <WorkLedgerView />
    </div>
  );
}
