import { BulkTransferView } from '@/components/bulk-transfer/bulk-transfer-view';

export default function BulkTransferPage() {
  return (
    <div className="bulk-transfer-page flex flex-1 flex-col min-h-0 px-8 py-8 w-full">
      <BulkTransferView />
    </div>
  );
}
